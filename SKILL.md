---
name: enterprise-research
description: "输入企业名称，生成一份包含基本信息、行业信息、待遇情况和地域岗位补充信息的企业调研报告（Markdown文件）。通过企查查Connector获取工商登记、财务数据、年报等权威数据，并通过网络搜索补充行业前景、竞争格局、员工评价、工作时长等信息。当用户需要对企业进行全面背调、求职前调研、或生成企业信息报告时触发此技能。触发词：查企业、企业调研、公司背调、企业信息报告、company research、帮我查一下XX公司。"
agent_created: true
---

# 企业信息调研报告生成技能

## 概述

输入企业名称后，通过企查查 Connector 获取权威工商/财务数据，结合网络搜索补充行业分析、员工评价、工作时长等信息，最终生成一份结构化的 Markdown 格式企业调研报告。

**核心原则：实事求是。** 所有信息必须有据可查；查不到的字段如实标注"查不到"，绝不编造。

---

## 工作流程

### 第一步：确认企业名称

确认用户输入的企业名称。若用户只提供简称或缩写，主动询问确认企业全称。查询前向用户说明：企查查提供工商/财务等权威数据，行业分析、员工评价等将通过网络搜索补充。

### 第二步：企查查数据获取

加载 `qcc-company` 技能，依次调用以下企查查工具（按需调用，若某工具返回为空则跳过）：

| 序号 | 工具 | 用途 | 对应报告模块 |
|------|------|------|-------------|
| 1 | `get_company_profile` | 企业简介、主营业务、所属行业 | 基本信息 + 行业信息 |
| 2 | `get_company_registration_info` | 成立日期、注册资本、法定代表人、登记状态、经营范围 | 基本信息 |
| 3 | `get_financial_data` | 近年营收、利润、资产负债率等财务指标 | 基本信息 |
| 4 | `get_annual_reports` | 年报中的从业人数、经营状态 | 基本信息（在职人数） |
| 5 | `get_listing_info` | 上市信息、股票代码、市值 | 基本信息（融资情况） |
| 6 | `get_shareholder_info` | 股东构成、持股比例 | 基本信息（融资情况） |
| 7 | `get_external_investments` | 对外投资记录 | 基本信息（补充） |
| 8 | `get_branches` | 分支机构（关注用户指定城市是否有分支机构） | 补充信息 |

**数据忠实原则：** 严格引用企查查返回的原始字段值，不推导、不编造。金额注明单位（元/万元），日期以 YYYY-MM-DD 格式呈现。若工具返回为空或无记录，如实标注。

### 第三步：行业与竞争分析（网络搜索）

使用 WebSearch 工具搜索以下内容：

1. **行业归属**：根据企查查返回的行业分类，确认企业所属的具体行业
2. **行业发展前景**：搜索"[行业名称] 行业发展前景 2024 2025"，了解行业趋势、市场规模、增长潜力
3. **竞争程度**：搜索"[行业名称] 竞争格局 行业排名"，了解行业竞争激烈程度、头部企业

搜索时优先使用中文关键词，信息源优先选择行业报告、权威媒体报道。

### 第四步：待遇与员工评价调研（网络搜索）

使用 WebSearch 工具搜索以下内容：

1. **工作时长/是否双休**：搜索"[企业名称] 加班 双休 工作时间"，关注脉脉、看准网、BOSS直聘等平台的员工反馈
2. **网上员工评价**：搜索"[企业名称] 员工评价 怎么样"，综合多个来源的评价，注意区分整体评价和个别吐槽
3. **薪酬水平**：搜索"[企业名称] 薪资 待遇"，了解大致薪酬范围（如有）

搜索结果中，注意标注信息来源平台名称（如"据脉脉平台反馈"、"据看准网数据"等），便于用户自行核实。

**岗位聚焦规则**：若搜索结果涉及多种岗位/工种（如行政岗、产线岗、研发岗等），且用户未明确指定查询岗位类型，则**聚焦研发岗**（技术/开发/算法/产品等），排除行政岗、产线岗、销售岗等非技术岗位。如用户指定了特定岗位，以用户指定为准。

### 第五步：地域岗位补充信息（网络搜索）

**默认规则**：若用户未指定就业地点和具体岗位，默认查询**杭州**的**测试和测开（测试开发）**岗位。若用户有明确输入（如"北京 开发"、"深圳 AI算法"），以实际输入为准，保留用户指定的地点即可，不再设次选城市。

使用 WebSearch 搜索：

1. 搜索"[企业名称] {{用户指定地点/默认杭州}} {{用户指定岗位/默认测试}} 招聘"，了解该企业在该城市是否有相关岗位招聘
2. 若企查查分支信息显示该企业在目标城市有分支机构，结合分支机构信息补充说明
3. 搜索"[企业名称] {{用户指定岗位/默认测试}} 岗位 薪资"，了解相关岗位的薪酬范围（如有）

**注意：** 仅提供搜索到的客观信息，不做主观推荐。若搜索不到相关岗位信息，如实说明"未找到该企业在{{目标城市}}的{{目标岗位}}公开信息"。

### 第六步：企业多维评分

依据前五步已收集的数据，对企业按以下 8 个维度打分（每项 0-10 分整数），作为第七步生成报告的输入。

#### 评分维度

| 维度 | 评分依据 |
|------|---------|
| **发展前景** | 行业增长趋势、公司市场地位、业务增速。龙头+高增长=高分。 |
| **稳定性** | 成立时长、融资/上市情况、营收稳健性、抗风险能力。上市公司+低负债=高分。 |
| **薪资竞争力** | 测试/测开岗位薪资 vs 目标城市同类岗位市场水平。 |
| **工作生活平衡** | 是否双休、加班强度、弹性工作制。双休且基本不加班=高分，强制加班/刷时长=低分。 |
| **福利完善度** | 五险一金（公积金比例越高越好）、年终奖（是否写入合同）、补贴、期权。 |
| **技术成长性** | 技术栈先进性、研发投入占比、测试岗能接触的核心技术深度。 |
| **岗位匹配度** | 目标城市测试&测开岗位数量、是否在招、岗位方向是否匹配。 |
| **公司规模** | 员工人数、业务覆盖范围、品牌知名度。千人以上+全球化=高分。 |

#### 评分规则

- 依据前五步已收集的数据直接打分，**不额外搜索**
- 每项给出 1-2 句简短理由
- 低于 4 分的"硬伤"维度和高于 8 分的"亮点"维度做一句话点评
- 每项得分必须为整数（0-10）
- 记录评分结果（含理由），供第七步写入 Markdown 和 HTML

### 第七步：生成报告与对比看板

#### 7.1 生成 Markdown 报告

加载 `references/output_template.md` 模板，将前五步数据和第六步评分结果填入模板，保存为 `企业调研报告_[企业名称].md`。

#### 7.2 生成/更新 HTML 企业对比看板

**关键**：每次查询一家新企业，都要更新同一个 HTML 文件，实现企业累积和横向对比。

**固定文件路径**：工作目录下 `企业对比看板.html`。

**操作流程**：

1. 检查 `企业对比看板.html` 是否存在
2. **若不存在**：加载 `references/dashboard_template.html` 作为骨架，填入当前企业数据，创建新文件
3. **若已存在**：读取现有 HTML，追加新企业的 tab 和数据

**HTML 看板结构要求**：

- **顶部**：标题栏 + 已收录企业数量
- **左侧**：企业列表（垂直 tab，点击切换）
- **右侧主体区域（默认视图）**：
  - 使用 Chart.js 雷达图，将**所有已收录企业**的 8 个维度评分叠加对比
  - 用不同颜色区分各企业
  - 图表下方：企业对比汇总表（行=企业，列=8 维度+总分+均分），最高分绿色高亮
- **点击某企业 tab**：右侧切换为单企业详情页，包含：
  - 企业名称标题 + 「📄 查看完整报告」按钮（蓝底白字，点击弹出模态窗以 Markdown 格式渲染对应 MD 调研报告）
  - 单企业水平条形图（8 维度，绿/橙/红三色）
  - 评分明细表（维度 | 得分 | 简评）
  - 基本信息摘要（成立时间、规模、融资/上市状态、所在城市、目标岗位状态）

**HTML 看板交互功能**：

看板内置以下交互操作：

1. **查看完整报告**：企业详情 tab 中点击「📄 查看完整报告」按钮，弹出模态窗口，通过 `marked.js` 将对应 MD 调研报告渲染为格式化 HTML 显示。需先复制 `references/server.js` 到 workspace 根目录并启动（端口 8765）以提供 MD 文件 HTTP 访问。若服务未运行，弹窗会提示「无法加载报告」并给出 `node server.js` 启动命令。
2. **从 MD 导入企业**：左下角「＋ 从 MD 导入企业」按钮，点击后选择本地 `.md` 调研报告文件，自动解析企业名称、8 维评分、简评、成立/规模/融资状态、城市、岗位等信息并添加到看板。若企业已存在则提示覆盖。
3. **删除企业**：鼠标悬停企业列表中的企业项时，右侧出现 × 删除按钮，点击后确认即可从看板移除（不影响本地 MD 文件）。
4. **维度排除与重计分**：详细对比表每个维度列表头旁有红色 ✕ 按钮，点击可排除该维度（✓ 为恢复），其余维度重算总分/均分，雷达图、总分排名柱状图同步更新；表头标题显示当前满分基数，标题旁提示已排除维度并提供「恢复全部」。至少保留 1 个维度。
5. **表头排序**：每个列（企业/各维度/总分/均分）表头带清晰排序按钮（↕ 默认、↑/↓ 激活蓝底），点击三次循环：降序 → 升序 → 取消排序回到默认顺序。排序按钮与维度排除 ✕ 按钮间距拉开避免误触。

**HTML 技术约束**：
- 使用 Chart.js v4.4.1，CDN：`cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.js`
- 使用 marked.js v14.1.0，CDN：`cdnjs.cloudflare.com/ajax/libs/marked/14.1.0/marked.min.js`
- 配色方案：绿 `#639922` / 橙 `#EF9F27` / 红 `#E24B4A`
- 所有企业评分数据以 JavaScript 数组形式嵌入 HTML 中（`<script>` 标签内），便于动态渲染
- 企业详情（基本信息摘要）以 HTML 内联形式写入，每个企业一个 `<div>`，默认隐藏
- 需运行 `references/server.js`（本地 Node.js 静态文件服务器，端口 8765）以提供 MD 文件的 HTTP 访问。使用时先将其复制到 workspace 根目录，在 WorkBuddy 终端运行 `node server.js`（自带 managed Node，无需额外安装）启动；该后台进程在系统/会话重启后可能停止，需要时重新运行。HTML 中 `openReport()` 函数通过 `fetch("http://localhost:8765/企业调研报告_xxx.md")` 获取 MD 内容，`marked.parse()` 渲染为 HTML 后插入模态窗口；若服务不可达，弹窗提示未运行并给出 `node server.js` 启动命令。
- MD 导入使用 `FileReader` 读取本地文件，`parseMDReport()` 解析评分表/基本信息，无需后端
- **localStorage 持久化**：看板使用 localStorage 缓存用户操作（增删企业、❤️/💔 状态），页面加载时优先从 localStorage 恢复。`initData()` 函数会合并硬编码默认数据，确保新增企业自动出现在看板中

**输出顺序**：

1. 生成 Markdown 报告 → 2. 生成/更新 HTML 看板 → 3. `present_files` 同时展示 MD + HTML

---

## 输出规范

- **实事求是**：所有信息必须有据可查。企查查数据标注数据来源为"企查查"；网络搜索信息标注来源平台名称
- **空数据处理**：若某项信息查不到，如实填写"查不到"，不编造、不推测
- **信息时效提示**：工商登记数据实时同步（T+0）；财务数据和年报依赖企业公示周期，可能存在滞后；网络评价信息具有主观性，仅供参考
- **结构化呈现**：按模板结构组织内容，使用 Markdown 标题、列表、表格等格式

---

## 注意事项

- 企查查数据覆盖以**中国境内工商登记企业**为主，境外企业可能无记录
- 财务数据和年报依赖企业公示周期，非上市企业可能无财务数据
- 员工评价信息来自网络公开平台，具有主观性，需综合判断
- 地域岗位信息为搜索时点状态，招聘信息具有时效性
